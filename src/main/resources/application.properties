## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:postgresql://localhost:5454/CronJobs
spring.datasource.username= cron_rw_user
spring.datasource.password= 7&W-%?cc

# The SQL dialect for Hibernate
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# SQL standard output
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# The liquibase properties
spring.liquibase.change-log = classpath:/db/liquibase-changelog.xml

# Fix for LOB creation
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Add OData params example
odata.authentication.header.name = auth.odata.header
odata.authentication.header.value = upi.auth.odata.value